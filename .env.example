# Redis (Upstash, via Vercel Marketplace: Storage → Create Database → Upstash Redis)
KV_REST_API_URL=
KV_REST_API_TOKEN=

# Blob storage (Vercel: Storage → Create Database → Blob)
BLOB_READ_WRITE_TOKEN=

# Painel /admin
ADMIN_EMAIL=
ADMIN_PASSWORD=
SESSION_SECRET=
